#ifndef LEXER_H
#define LEXER_H

typedef struct {
    // Write the correct struct
} TokenList;

// Implement the tokenize and wire in the previous code
TokenList tokenize(char *input);
void free_tokens(TokenList *tokens);

#endif
